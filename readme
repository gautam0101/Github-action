Steps for docker:-
create a folder and initialize the npm in the folder
It creates the package.json file, package-lock.json file.
install express js
create a server.js file in the node-app
Create a Dockerfile
build the docker image
check the images in the
open the browser and check

For create AWS ECR we:-
Login to account 
Search for ECR 
And create repo
And add the user access id to github

For EKS:- 
for install
brew tap weaveworks/tap
brew install weaveworks/tap/eksctl

for cluster
eksctl create cluster --name my-cluster --region ap-south-1 --nodegroup-name linux-nodes  --node-type t2.micro --nodes 2

for config 
aws eks update-kubeconfig --region ap-south-1 --name my-cluster

kubectl get nodes
# This will give the the nodes you have in the cluster as output

for deploy

kubectl apply -f  kube.yaml service.yaml